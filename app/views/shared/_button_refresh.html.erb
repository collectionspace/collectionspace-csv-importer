<%= content_tag :span, class: classes do %>
  <%= local_assigns.fetch(:text, t('action.refresh')) %>
<% end %>
<%= link_to '#',
    id: local_assigns.fetch(:id, SecureRandom.hex),
    class: 'refresh tag',
    data: data, onclick: spinner_js do %>
  <%= fa_icon "refresh" %>
<% end %>
